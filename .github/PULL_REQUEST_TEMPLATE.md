## What this changes

<!-- The behaviour, not the diff. What someone using the app would notice. -->

## Why

<!-- Link the issue if there is one: Fixes #123. If there is not, say what went
     wrong or what was missing. -->

## Checks

<!-- Tick what you ran. Leave the rest unticked rather than guessing. -->

- [ ] `npm run typecheck`
- [ ] `npm run lint`
- [ ] `npm run format:check`
- [ ] `npm test`
- [ ] `npm run build`
- [ ] Touched `src/lib/crypto.ts`, so a test covers the new behaviour
- [ ] Runes produced before this change still decode, or the change is marked
      breaking and is in the changelog under that heading

## Anything left undone

<!-- A known limit, a case not covered, a follow-up worth its own issue. Saying
     so is better than it being found later. -->
